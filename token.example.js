"use strict";

// Copy this file outside the module directory, restrict its permissions, and
// set config.tokenFile to its absolute path. Never commit the copied file.
module.exports = {
  token: "replace-with-a-music-assistant-long-lived-token"
};
